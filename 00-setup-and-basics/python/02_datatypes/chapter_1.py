sugar_amount = 2
print(f"Initial sugar: {sugar_amount}")

sugar_amount = 12
print(f"Second Initial sugar: {sugar_amount}")

print(f"ID of 2: {id(2)}")
print(f"ID of 12: {id(12)}")
 # Number is immutable in Python, so the ID changes when the value changes
 # Id mean memory address where the object is stored
 # so number 2 is stored at one memory location and number 12 is stored at another memory location , they just happen to be of same datatype and has same reference 
