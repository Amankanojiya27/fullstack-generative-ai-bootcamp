# Learn Generators

# yield      → returns a value and pauses function
# next()     → resumes generator and gets next value
# send()     → sends value into generator
# yield from → delegates to another generator
# close()    → stops generator execution
